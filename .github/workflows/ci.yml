name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
      
      - run: uv sync
      - run: uv run ruff check
      - run: uv run ty check
      - run: uv run pytest